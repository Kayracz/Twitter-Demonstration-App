<nav class="navbar navbar-inverse">
   <div class="container">
      <div class="navbar-header">
         <%= link_to 'Twitter Followers', root_path, class: 'navbar-brand' %><i class="fab fa-twitter"></i>
      </div>
      <div id="navbar">
         <% if current_user %>
         <ul class="nav navbar-nav pull-right">
            <span class="class-img"><%= image_tag current_user.image_url, alt: current_user.name %></span>
            <li><%= link_to 'Log Out', logout_path, method: :delete %></li>
         </ul>
         <% else %>
         <ul class="nav navbar-nav">
            <div id="ex1" class="modal">
               <h2>Instructions</h2>
               <p>1. Sign-in and authenticate your twitter account</p>
               <p>2. Twitter will gather your profile and display a list of your followers.</p>
               <p>2. The followers gathered will be stored in your account everytime you login </p>
            </div>
            <p><a href="#ex1" rel="modal:open"><i class="far fa-question-circle"></i></a></p>
         </ul>
         <% end %>
      </div>
   </div>
</nav>
